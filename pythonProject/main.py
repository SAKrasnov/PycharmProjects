import eel # Импортируем нашу библиотеку

eel.init('interface') # Подключаем папку с интерфейсом
eel.start('Web.html', size=(500, 500)) # ell.start - подключаем основной файлик, size=(500, 500) - размеры приложения при открытии.